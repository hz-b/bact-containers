Bootstrap: localimage
From: ../build/ubuntu-build.sif

%post
    export TOP_DIR=`pwd`
    mkdir build
    cd build
    export BUILD_DIR=`pwd`

    # setup pip
    pip3 install --upgrade pip wheel
    pip3 install numpy pandas setuptools python-dateutil

    
%runscript
    exec python3 "$@"
